<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chilldrenofpatria">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StayAndWatchBakeryContinue"></activity>
        <activity android:name=".GoToTownHallContinue" />
        <activity android:name=".GoToTownHall" />
        <activity android:name=".StayAndWatchBakery" />
        <activity android:name=".Chapter1Contiinue" />
        <activity
            android:name=".Chapter1Activity"
            android:label="@string/title_activity_chapter1"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Book1Activity"
            android:label="@string/title_activity_book1"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".HomeActivity"/>
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>